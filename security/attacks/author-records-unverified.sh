#!/usr/bin/env bash
# author-records-unverified (BOUNDARY): plankton author records hash(--out) + the --cmd string; it NEVER runs the command. By design (SPEC 5).
echo "BOUNDARY (by design, SPEC 5): plankton is a recording ledger; trust that a computation is honest belongs to a trusted executor / re-run. Not a kernel defect."
