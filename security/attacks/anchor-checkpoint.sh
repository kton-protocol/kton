#!/usr/bin/env bash
# anchor-checkpoint: inclusion proof reported verified while the signed tree-head checkpoint was not validated. Pinned-key fix 9cb7090; checkpoint residual.
echo "hostile endpoint presents an attacker-chosen rootHash; inclusion verifies against it. Pinned-key 9cb7090 stops self-served-key fabrication; verify the signed checkpoint too. Local mock only."
