#!/usr/bin/env bash
# federation-withholding (BOUNDARY): a verifier only sees sources it names; a withheld source/revocation removes nothing it should. Documented closed-world limit.
echo "BOUNDARY (documented): resolvability is relative to the source list; a verdict must NAME the sources it consulted. Withholding has leverage only where a judgment is closed-world (the gate)."
