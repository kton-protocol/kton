#!/usr/bin/env bash
# wasm-viewer-trust: the viewer WASM trusted the declared fotonId/keyid (no recompute) and short()=48-bit id collision. FIXED 6ff639e.
echo "graph.wasm rendered planted/tampered records under their declared id; FIXED 6ff639e (re-derive id + 128-bit node identity). Drive with node."
